<%= form_for @folder2, :html => {:class => 'form-group'} do | f | %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit 'Add Folder', :class => "btn btn-secondary" %>
<% end %> 

